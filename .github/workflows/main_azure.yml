name: Deploy Microservice to Azure

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout del código
        uses: actions/checkout@v4

      - name: 2. Configurar Java JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: 'maven'

      - name: 3. Compilar con Maven
        run: mvn clean package -DskipTests
        working-directory: ./microservicio-demo

      - name: 4. Login en Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 5. Build y Push de Imagen Docker
        run: |
          cd microservicio-demo
          echo "FROM eclipse-temurin:17-jre-alpine" > Dockerfile
          echo "WORKDIR /app" >> Dockerfile
          echo "COPY target/*.jar app.jar" >> Dockerfile
          echo "EXPOSE 8082" >> Dockerfile
          echo "ENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]" >> Dockerfile
          
          docker build -t ${{ secrets.DOCKER_USERNAME }}/ijaramillo-msdemo:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/ijaramillo-msdemo:latest

      - name: 6. Despliegue en Azure vía SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.AZURE_IP }}
          username: azure-jaramillo
          key: ${{ secrets.AZURE_SSH_KEY }}
          script: |
            cd ~/proyecto-azure
            docker-compose pull
            docker-compose up -d